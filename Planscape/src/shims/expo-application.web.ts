// Web stub — expo-application reads native app metadata unavailable in browsers.
export const applicationId: string | null = null;
export const applicationName: string | null = 'Planscape';
export const nativeApplicationVersion: string | null = null;
export const nativeBuildVersion: string | null = null;
export async function getInstallationTimeAsync(): Promise<Date> { return new Date(); }
export async function getLastUpdateTimeAsync(): Promise<Date | null> { return null; }
