{
  "name": "planscape-cron",
  "version": "1.0.0",
  "private": true,
  "description": "Planscape billing lifecycle cron Worker (trial expiry, reminders, dunning, digest, cleanup).",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250510.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
